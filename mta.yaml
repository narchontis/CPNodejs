ID: CPnodejs
_schema-version: "2.1"
description: testing
version: 0.0.1
modules:
  - name: CPnodejs-db
    type: hdb
    path: db
    parameters:
      memory: 256M
      disk-quota: 256M
    requires:
      - name: CPnodejs-hdi-container
  - name: CPnodejs-srv
    type: nodejs
    path: srv
    parameters:
      memory: 512M
      disk-quota: 256M
    provides:
      - name: srv_api
        properties:
          url: ${default-url}
    requires:
      - name: CPnodejs-hdi-container
resources:
  - name: CPnodejs-hdi-container
    type: com.sap.xs.hdi-container
    properties:
      hdi-container-name: ${service-name}
